# Files Prettier should not format.

# Google Search Console verification file — must contain only the verification string.
# https://search.google.com/search-console/welcome
docs/google6aafae9f5e2ed07d.html

docs/tools/index.html

# TypeDoc output — generated by `npm run docs:reference`, never hand-edited.
# Same rule as docs/tools/index.html above: we format what we write, not what
# a generator emits. (docs/reference is likewise absent from validate-html's
# target list; its markup is TypeDoc's to answer for, not ours.)
docs/reference/

