/**
 * Conformance battery for any `Parser` implementation.
 *
 * When filled in, this file exports `runParserConformance(parser, suiteName)`
 * which any parser implementation — odf-kit's default or a third-party
 * adapter — can be tested against. See `ADAPTERS.md` for usage.
 *
 * Stub — the battery is added in Phase 3 of v0.13.2, alongside the
 * `parseXml` tightening and the `odfKitParser` export.
 */

test.todo("parser conformance battery — added in Phase 3");
