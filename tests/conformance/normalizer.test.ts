/**
 * Conformance battery for any `Normalizer` implementation.
 *
 * When filled in, this file exports `runNormalizerConformance(normalizer,
 * suiteName)` which any normalizer implementation — odf-kit's default or
 * a third-party substitute — can be tested against. See `ADAPTERS.md` for
 * usage.
 *
 * Stub — the battery is added in Phase 2 of v0.13.2, alongside the Tier 1
 * normalizer and `odfKitNormalizer` export.
 */

test.todo("normalizer conformance battery — added in Phase 2");
